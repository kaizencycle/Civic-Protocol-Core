# C-303: pinned from floating versions — was: fastapi, uvicorn, httpx, pydantic (all unpinned)
# Floating deps in production microservices cause silent breaking changes on redeploy.

fastapi==0.112.2
uvicorn[standard]==0.30.5
httpx==0.27.2
pydantic==2.9.1
fastapi-mcp-router>=0.3.0,<0.4.0
ipfshttpclient>=0.7.0,<0.8
base58>=2.1.1
