services:
  - type: web
    name: mobius-mic-wallet
    env: python
    plan: free
    region: oregon
    runtime: python
    rootDir: mic-wallet
    buildCommand: |
      pip install -r requirements.txt
    startCommand: |
      uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    # C-360 / C-379: persistent disk — without this, wallet balances reset every deploy.
    # Live service mobius-mic-wallet-service was missing this block (deploy logs 2026-07-21).
    disk:
      name: mic-wallet-data
      mountPath: /var/lib/mic-wallet
      sizeGB: 1
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        sync: false
      - key: PORT
        value: "8001"
